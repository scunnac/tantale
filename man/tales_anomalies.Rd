% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tales_class.R
\name{tales_anomalies}
\alias{tales_anomalies}
\title{Report the biological anomalies in a tales object}
\usage{
tales_anomalies(x)
}
\arguments{
\item{x}{A \code{\link{tales}} object.}
}
\value{
A tibble of \code{array_id}, \code{check} and \code{detail}, one
row per anomaly. Zero rows if the object is clean.
}
\description{
Lists the arrays that are \emph{odd} rather than \emph{unreadable}: missing sequence
data, impossible domain-type arrangements, coordinate disagreements, an
amino acid sequence paired with more than one RVD, or an attribute that
varies within an array when it should not.

Such arrays are accepted by \code{\link{tales}} -- real TALE predictions are
messy, and refusing to load them would force cleaning outside the package and
destroy the diagnostic signal. Construction warns about them; this function
tells you which and why; \code{tales(x, sanitize = TRUE)} removes them.
}
\examples{
# A2 carries two N-termini -- an impossible arrangement.
odd <- data.frame(
  array_id = c("A1", "A1", "A2", "A2", "A2"),
  position_in_array = c(1L, 2L, 1L, 2L, 3L),
  domain_type = c("N-terminus", "repeat",
                  "N-terminus", "N-terminus", "repeat"),
  rvd = c("NTERM", "HD", "NTERM", "NTERM", "NI")
)
x <- suppressWarnings(tales(odd))
tales_anomalies(x)
tales(odd, sanitize = TRUE) # drops A2 instead of merely warning
}
\seealso{
Other tales objects:
\code{\link[=as_tales]{as_tales()}},
\code{\link[=format.tales]{format.tales()}},
\code{\link[=format.tales_msa]{format.tales_msa()}},
\code{\link[=is_tales]{is_tales()}},
\code{\link[=new_tales]{new_tales()}},
\code{\link[=print.tales]{print.tales()}},
\code{\link[=print.tales_msa]{print.tales_msa()}},
\code{\link[=summary.tales]{summary.tales()}},
\code{\link[=summary.tales_msa]{summary.tales_msa()}},
\code{\link[=tales]{tales()}},
\code{\link[=tales_anchor_codes]{tales_anchor_codes()}},
\code{\link[=tales_assert_complete]{tales_assert_complete()}},
\code{\link[=tales_bind]{tales_bind()}},
\code{\link[=tales_namespace]{tales_namespace()}},
\code{\link[=tales_requirements]{tales_requirements()}},
\code{\link[=validate_tales]{validate_tales()}}
}
\concept{tales objects}
