% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distalr.R
\name{tales_assign_domain_codes}
\alias{tales_assign_domain_codes}
\title{Assign a domain code to every distinct part sequence}
\usage{
tales_assign_domain_codes(x)
}
\arguments{
\item{x}{A \link{tales} object carrying an \code{aa_seq} column.}
}
\value{
\code{x} with a \code{dom_code} column and a \code{dom_code_namespace} stamp.
}
\description{
Step 1 of \code{\link[=tales_compare]{tales_compare()}}. Gives each distinct \code{aa_seq} in \code{x} an
integer code, recorded in a \code{dom_code} column, so that the parts can be
compared once each rather than once per occurrence.

A code names a distinct \strong{domain} sequence, not a repeat: the N- and
C-termini are parts like the repeats are, and they get codes too.
}
\details{
TALEs reuse domains heavily, within an array and between arrays, so the
number of distinct sequences is far smaller than the number of parts. That
ratio is what makes \code{\link[=tales_domain_distances]{tales_domain_distances()}} affordable -- it compares
distinct domains, not parts.
}
\section{The codes are only meaningful within one call}{

Codes are assigned with \code{\link[dplyr:cur_group_id]{dplyr::cur_group_id()}} over the distinct \code{aa_seq}
values \strong{present in \code{x}}. Add an array, remove one, or reorder the
sequences, and the same protein can get a different number. They are
positions in this table's own vocabulary, not identifiers of anything.

So \strong{comparing codes between two calls is an error}, and a similarity
table keyed by one call's codes must never be used with another call's.
This is not a caution to remember: it is enforced. Every object minted
here is stamped with a \code{dom_code_namespace} derived from the sequences
that produced it, and the classes refuse to join across namespaces. Read
the stamp with \code{\link[=tales_namespace]{tales_namespace()}}.
}

\seealso{
\code{\link[=tales_compare]{tales_compare()}}, which runs all three steps;
\code{\link[=tales_domain_codes]{tales_domain_codes()}}, which reads the code-to-sequence table back out
of an object that already has codes.

Other pairwise distances:
\code{\link[=as.matrix.pairwise_distances]{as.matrix.pairwise_distances()}},
\code{\link[=distances_assert_square]{distances_assert_square()}},
\code{\link[=distances_restrict]{distances_restrict()}},
\code{\link[=is_pairwise_distances]{is_pairwise_distances()}},
\code{\link[=pairwise_distances]{pairwise_distances()}},
\code{\link[=tales_compare]{tales_compare()}},
\code{\link[=tales_domain_distances]{tales_domain_distances()}},
\code{\link[=tales_group]{tales_group()}},
\code{\link[=tales_tale_distances]{tales_tale_distances()}},
\code{\link[=validate_pairwise_distances]{validate_pairwise_distances()}}
}
\concept{pairwise distances}
