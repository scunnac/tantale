% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distalr.R
\name{tales_tale_distances}
\alias{tales_tale_distances}
\title{Pairwise distances between TALE arrays}
\usage{
tales_tale_distances(x, domain_distances)
}
\arguments{
\item{x}{A \link{tales} object carrying \code{dom_code}.}

\item{domain_distances}{A \link{domain_distances} object for the same \code{x}, as
returned by \code{\link[=tales_domain_distances]{tales_domain_distances()}}.}
}
\value{
A \link{tale_distances} object, keyed by \code{array_id}.
}
\description{
Step 3 of \code{\link[=tales_compare_distal]{tales_compare_distal()}}. Aligns the arrays against each other as
strings of domain codes, using the domain distances from step 2 as the
cost of substituting one domain for another.
}
\details{
The two arguments are not independent, and that is the point. ARLEM aligns
each array's sequence of \code{dom_code}s; what it costs to align one domain
against a different one is taken from \code{domain_distances}, so the TALE-level
comparison is built on the domain-level one rather than computed beside it.

The domain distances are first passed through a Minkowski distance
(\code{p = 3.5}) between their rows and rescaled to 0-100. That step is not
cosmetic: ARLEM needs a cost matrix satisfying the triangle inequality,
and raw pairwise alignment dissimilarities do not.
}
\section{Both arguments must come from the same call}{

\code{domain_distances} is keyed by \code{dom_code}, and those codes mean what they
mean only within the call that minted them
(\code{\link[=tales_assign_domain_codes]{tales_assign_domain_codes()}}). Passing distances from one run with codes
from another silently compares the wrong domains, which is why both
objects carry a namespace stamp and this function refuses when they
disagree.
}

\examples{
x <- tales_from_telltale(system.file("extdata", "tellTaleExampleOutput",
                                     package = "tantale"))
xa <- tales_assign_domain_codes(x)
dd <- tales_domain_distances(xa)
tales_tale_distances(xa, dd)
}
\seealso{
\code{\link[=tales_compare_distal]{tales_compare_distal()}}, which runs all three steps.

Other pairwise distances:
\code{\link[=as.matrix.pairwise_distances]{as.matrix.pairwise_distances()}},
\code{\link[=distances_assert_square]{distances_assert_square()}},
\code{\link[=distances_restrict]{distances_restrict()}},
\code{\link[=is_pairwise_distances]{is_pairwise_distances()}},
\code{\link[=pairwise_distances]{pairwise_distances()}},
\code{\link{rvd_dna_specificity}},
\code{\link[=tales_assign_domain_codes]{tales_assign_domain_codes()}},
\code{\link[=tales_compare_distal]{tales_compare_distal()}},
\code{\link[=tales_compare_functal]{tales_compare_functal()}},
\code{\link[=tales_domain_distances]{tales_domain_distances()}},
\code{\link[=tales_group_hclust]{tales_group_hclust()}},
\code{\link[=tales_group_kmedoids]{tales_group_kmedoids()}},
\code{\link[=validate_pairwise_distances]{validate_pairwise_distances()}}
}
\concept{pairwise distances}
