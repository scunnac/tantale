% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tales_msa_class.R
\name{plot.tales_msa}
\alias{plot.tales_msa}
\title{Plot a TALE multiple alignment}
\usage{
\method{plot}{tales_msa}(x, fill = NULL, label = NULL, tal_sim = NULL, domain_sim = NULL, ...)
}
\arguments{
\item{x}{A \code{\link{tales_msa}} object.}

\item{fill}{Layer whose values colour the cells. Defaults to
\code{"dom_code"} when present, otherwise \code{"rvd"}.}

\item{label}{Layer whose values are written in the cells, or \code{NULL} for
none. Defaults to \code{"rvd"} when it is not already the \code{fill}.}

\item{tal_sim}{Optional \code{\link{tale_distances}} (or legacy table) used to
order arrays by overall similarity.}

\item{domain_sim}{Optional \code{\link{domain_distances}} (or legacy table) of
similarities between domains, used to colour cells.}

\item{...}{Passed to \code{\link{plot_tales_msa}}: \code{h_cut},
\code{ref_pattern}, \code{consensus}, \code{fill_type}.}
}
\value{
The plot object returned by \code{\link{plot_tales_msa}}.
}
\description{
Draws the alignment as a heatmap: one row per array, one column per
alignment position, cells coloured by one layer and optionally labelled with
another.
}
\details{
A \code{tales_msa} carries every layer at once, so the layers are named
rather than passed as separate matrices — which is what
\code{\link{plot_tales_msa}} required, a matrix being able to hold only one.
}
\seealso{
Other TALE plots:
\code{\link[=msa_heatmap]{msa_heatmap()}},
\code{\link[=plot.tales]{plot.tales()}},
\code{\link[=plot_tales_composition]{plot_tales_composition()}},
\code{\link[=plot_target_preds]{plot_target_preds()}},
\code{\link[=talomes_heatmap]{talomes_heatmap()}}
}
\concept{TALE plots}
