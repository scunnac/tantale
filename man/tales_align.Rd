% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tales_msa_class.R
\name{tales_align}
\alias{tales_align}
\title{Align the repeat arrays of a tales object}
\usage{
tales_align(
  x,
  residue_col = c("rvd", "dom_code"),
  repeat_sims = NULL,
  mafft_opts = "--localpair --maxiterate 1000 --reorder --op 0 --ep 5 --thread 1",
  mafft_path = NULL,
  mafft_verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{\link{tales}} object holding complete arrays.}

\item{residue_col}{Which layer to align on: \code{"rvd"} (default) or
\code{"dom_code"}. Given explicitly rather than guessed from the values.}

\item{repeat_sims}{Scoring matrix for the residues being aligned.
\code{NULL} (default) means none. Pass \code{"rvd"} to opt in to the
built-in RVD similarity matrix when aligning RVDs, or a
\code{\link{domain_distances}} object when aligning repeat codes. Optional similarity table passed to MAFFT as a scoring
matrix, as accepted by \code{\link{tales_align}}.}

\item{mafft_opts}{Command-line options handed to MAFFT. The default,
\code{"--localpair --maxiterate 1000 --reorder --op 0 --ep 5 --thread 1"},
asks for an accurate local alignment and sets the gap penalties.

\code{--op} is the cost of opening a gap and \code{--ep} the cost of
extending one. They are set unusually here -- free to open, expensive to
extend -- because of what is being aligned. A TALE gains or loses whole
repeats, so a gap should start wherever it needs to; what should be
discouraged is one long gap swallowing a stretch of repeats that really
do correspond. Raise \code{--op} if the alignment fragments into too
many small gaps.}

\item{mafft_path}{Where to find MAFFT. \code{NULL}, the default, uses the
\code{tantale} conda environment, creating it on first use. Give the root
of a standalone MAFFT directory instead (one holding \code{mafft.bat}
with the helpers under \code{mafftdir/libexec}) to use your own copy --
but note that the version matters: MAFFT changed how it aligns text-mode
sequences after 7.4x, and later releases leave the termini of a TALE
alignment unanchored.}

\item{mafft_verbose}{Whether to let MAFFT write to the console. It reports
its banner, the strategy it chose and its progress through the sequences,
which is dozens of lines per alignment and rarely what you want. Left
\code{FALSE} that output is captured rather than discarded, and replayed
if the alignment fails -- so silence costs nothing diagnostically.}

\item{...}{Further arguments to the MAFFT runner, chiefly
\code{gap_symbol}, the value gaps take in the returned matrix
(\code{NA} by default).}
}
\value{
A \code{tales_msa} object.
}
\description{
Aligns TALE arrays on one of their residue layers with MAFFT's text mode,
returning the alignment as a \code{\link{tales_msa}} — the input object with
an \code{alignment_position} column added, so every other layer
(\code{rvd}, \code{dom_code}, \code{aa_seq}, ...) remains available.
}
\details{
The mapping back from MAFFT's output is \strong{positional}: the k-th non-gap cell
of an aligned row is the k-th part fed in. That is well defined only because
this function builds MAFFT's input from \code{x} itself, which is why
\code{\link{tales_assert_complete}} is enforced first.
}
\seealso{
Other TALE alignment:
\code{\link[=as.matrix.tales_msa]{as.matrix.tales_msa()}},
\code{\link[=is_tales_msa]{is_tales_msa()}},
\code{\link[=tales_consensus]{tales_consensus()}},
\code{\link[=tales_consensus_match]{tales_consensus_match()}},
\code{\link[=tales_msa]{tales_msa()}},
\code{\link[=tales_width]{tales_width()}},
\code{\link[=validate_tales_msa]{validate_tales_msa()}}
}
\concept{TALE alignment}
