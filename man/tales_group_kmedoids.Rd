% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{tales_group_kmedoids}
\alias{tales_group_kmedoids}
\title{Group TALEs by k-medoids clustering of their pairwise distance}
\usage{
tales_group_kmedoids(
  x,
  tal_sim,
  k_range = NULL,
  k = NULL,
  seed = 7,
  plot_silhouette = TRUE
)
}
\arguments{
\item{x}{A \link{tales} object -- the one whose comparison produced \code{tal_sim}.}

\item{tal_sim}{A \link{tale_distances} object, as returned by \code{\link[=tales_compare]{tales_compare()}}.
A plain data frame using the legacy \code{TAL1}/\code{TAL2}/\code{Sim} column names is
also accepted and coerced.}

\item{k_range}{Integer vector of candidate values of \code{k} to evaluate.}

\item{k}{See Details.}

\item{seed}{Passed to \code{set.seed()} before every \code{cluster::pam()}
call, so the same candidate always clusters the same way. Previously a
hardcoded \code{7}; now a documented, overridable default.}

\item{plot_silhouette}{Logical, whether to draw the silhouette-vs-k plot.}
}
\value{
\code{x} with an added (or replaced) \code{group} column.
}
\description{
Clusters TALE arrays with \code{\link[cluster:pam]{pam}} for every
candidate \code{k} in \code{k_range}, then returns the \code{\link{tales}}
object the distances were computed from with the chosen clustering's
result attached as its \code{group} column.
}
\details{
The clustering is computed from \code{tal_sim}, but the result belongs on the
\code{tales} object the distances were computed from, so that is what comes
back. \code{group} is a recognised \code{tales} column, validated as constant within
an array -- it is an array-level property, like \code{seqnames}.

Taking \code{x} rather than returning a bare lookup table is what makes the
correspondence checkable: the array names in \code{tal_sim} must be the array
names in \code{x}, and this is the only place that can be verified. A mismatch
is an error rather than a silent \code{NA} group, because a partly-grouped
object is the kind of thing that fails much later and confusingly.

The bare mapping is still one line away if you want it:
\code{unique(out[c("array_id", "group")])}.

Unlike a hierarchical tree, PAM has no single structure that can be cut at
an arbitrary \code{k} after the fact -- \code{k} is a parameter to the clustering
itself. So one clustering is computed per candidate in \code{k_range}, and \code{k}
picks which of those to keep:
\itemize{
\item \code{k} a single number uses that candidate directly.
\item \code{k = "auto"} picks the elbow of the silhouette-vs-k curve (a partial,
first-step application of the Kneedle algorithm -- see
\code{.tales_group_kmedoids_elbow()} -- good enough in practice to be worth
keeping, not a validated implementation of the full method).
\item \code{k = NULL} (the default) shows the silhouette plot and asks for a number
at the console -- but only when \code{\link{interactive}()} is \code{TRUE}.
In a script, a test or a vignette render, \code{k = NULL} errors instead of
blocking on input that will never arrive.
}
}
\examples{
x <- tales_from_telltale(system.file("extdata", "tellTaleExampleOutput",
                                     package = "tantale"))
cmp <- tales_compare(x)
grouped <- tales_group_kmedoids(cmp$tales, cmp$tale_distances,
                                k_range = 2:4, k = 2)
unique(grouped[c("array_id", "group")])
}
\seealso{
\code{\link[=tales_group_hclust]{tales_group_hclust()}}, the alternative method;
\code{\link[=tales_compare]{tales_compare()}}, which produces both inputs.

Other pairwise distances:
\code{\link[=as.matrix.pairwise_distances]{as.matrix.pairwise_distances()}},
\code{\link[=distances_assert_square]{distances_assert_square()}},
\code{\link[=distances_restrict]{distances_restrict()}},
\code{\link[=is_pairwise_distances]{is_pairwise_distances()}},
\code{\link[=pairwise_distances]{pairwise_distances()}},
\code{\link[=tales_assign_domain_codes]{tales_assign_domain_codes()}},
\code{\link[=tales_compare]{tales_compare()}},
\code{\link[=tales_domain_distances]{tales_domain_distances()}},
\code{\link[=tales_group_hclust]{tales_group_hclust()}},
\code{\link[=tales_tale_distances]{tales_tale_distances()}},
\code{\link[=validate_pairwise_distances]{validate_pairwise_distances()}}
}
\concept{pairwise distances}
