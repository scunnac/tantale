% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise_distances_class.R
\name{pairwise_distances}
\alias{pairwise_distances}
\alias{tale_distances}
\alias{domain_distances}
\title{Create a pairwise similarity table}
\usage{
pairwise_distances(x, dom_code_namespace = NULL)

tale_distances(x, dom_code_namespace = NULL)

domain_distances(x, dom_code_namespace = NULL)
}
\arguments{
\item{x}{A data frame with \code{id1}, \code{id2} and \code{dissim} columns.
Legacy spellings and the similarity vocabulary are accepted and converted:
a table carrying \code{Sim} or \code{normArlemScore} instead of a distance
is folded into \code{dissim}, and those restatements are then dropped so
only one copy of the quantity is stored. Further columns
(\code{arlem_score}, \code{max_length}, or anything else) are preserved.}

\item{dom_code_namespace}{Optional scalar string identifying the run whose
\code{dom_code} values this table is keyed by, see
\code{\link{tales_namespace}}. Relevant for \code{domain_distances()}, whose ids
\emph{are} \code{dom_code}s.}
}
\value{
A validated \code{pairwise_distances} object.
}
\description{
The long form of a square pairwise similarity over one entity set: one row
per ordered pair of entities, with a \code{sim} score.
}
\details{
\code{tale_distances()} and \code{domain_distances()} are the entity-specific flavours:
similarity between whole TALE arrays, and between individual repeat units.
They add no structure, only semantics — every method is written once on the
parent.

Legacy column spellings (\code{TAL1}/\code{TAL2}, \code{RepU1}/\code{RepU2},
\code{Sim}, \code{Dissim}, \code{arlemScore}, ...) are renamed on the way in.
}
\examples{
d <- data.frame(
  id1 = c("A1", "A1", "A2", "A2"),
  id2 = c("A1", "A2", "A1", "A2"),
  dissim = c(0, 35, 35, 0)
)
pairwise_distances(d)

# Legacy spellings are recognised and folded in.
legacy <- data.frame(TAL1 = "A1", TAL2 = "A2", Sim = 65)
tale_distances(legacy)
}
\seealso{
Other pairwise distances:
\code{\link[=as.matrix.pairwise_distances]{as.matrix.pairwise_distances()}},
\code{\link[=distances_assert_square]{distances_assert_square()}},
\code{\link[=distances_restrict]{distances_restrict()}},
\code{\link[=is_pairwise_distances]{is_pairwise_distances()}},
\code{\link[=tales_assign_domain_codes]{tales_assign_domain_codes()}},
\code{\link[=tales_compare]{tales_compare()}},
\code{\link[=tales_domain_distances]{tales_domain_distances()}},
\code{\link[=tales_group_hclust]{tales_group_hclust()}},
\code{\link[=tales_group_kmedoids]{tales_group_kmedoids()}},
\code{\link[=tales_tale_distances]{tales_tale_distances()}},
\code{\link[=validate_pairwise_distances]{validate_pairwise_distances()}}
}
\concept{pairwise distances}
