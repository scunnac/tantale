% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conversion.R
\name{split_list}
\alias{split_list}
\title{Split strings of TALE sequences (`sep`-separated rvd or distal repeat IDs)}
\usage{
split_list(strings, sep = "-")
}
\arguments{
\item{strings}{Either, the path to a fasta file, an AAStringSet or "BStringSet"
or a list. In all cases, each element of these objects is a string of a
tale sequence (`sep`-separated rvd or distal repeat IDs)}

\item{sep}{Separator of the elements of the sequence}
}
\value{
A list of named vectors representing the 'splited' sequence.
}
\description{
Load the content of fasta file containing TALE sequences (either RVD or Distal repeat code)
and return a list of vectors each one composed of the individual elements of the sequence.
}
\section{Deprecated}{

Superseded by \code{\link{as_tales}}, which returns a validated
\code{\link{tales}} object instead of a bare list, so array identity and
part order are carried by the data rather than by convention.
}

\seealso{
\code{\link{as_tales}}
}
