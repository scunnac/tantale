% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distalr.R
\name{tales_domain_distances}
\alias{tales_domain_distances}
\title{Pairwise distances between distinct TALE domains}
\usage{
tales_domain_distances(
  x,
  aln_method = "DECIPHER",
  ncores = 1,
  conda_bin = "auto"
)
}
\arguments{
\item{x}{A \link{tales} object carrying \code{dom_code}, as returned by
\code{\link[=tales_assign_domain_codes]{tales_assign_domain_codes()}}.}

\item{aln_method}{One of \code{"DECIPHER"} (the default), \code{"Biostrings"} or
\code{"mmseq2"}. \code{"mmseq2"} runs in the conda environment.}

\item{ncores}{Number of cores for the pairwise alignment.}

\item{conda_bin}{Passed to \code{reticulate}, for \code{aln_method = "mmseq2"}.}
}
\value{
A \link{domain_distances} object, stamped with \code{x}'s namespace.
}
\description{
Step 2 of \code{\link[=tales_compare_distal]{tales_compare_distal()}}. Aligns every distinct domain sequence against
every other and returns their pairwise dissimilarity.
}
\details{
This is the expensive step, and the one worth having on its own: the
repeat-level distances answer questions about repeat diversity that need
no TALE-level alignment at all, and computing them does not require
running ARLEM.

Distances are between \strong{distinct domains}, keyed by \code{dom_code}, so the
cost goes with the number of distinct sequences rather than the number of
parts. See \link[=summary.tales]{summary()} for that ratio on a given object.
}
\examples{
x <- tales_from_telltale(system.file("extdata", "tellTaleExampleOutput",
                                     package = "tantale"))
xa <- tales_assign_domain_codes(x)
tales_domain_distances(xa)
}
\seealso{
\code{\link[=tales_tale_distances]{tales_tale_distances()}}, which consumes this.

Other pairwise distances:
\code{\link[=as.matrix.pairwise_distances]{as.matrix.pairwise_distances()}},
\code{\link[=distances_assert_square]{distances_assert_square()}},
\code{\link[=distances_restrict]{distances_restrict()}},
\code{\link[=is_pairwise_distances]{is_pairwise_distances()}},
\code{\link[=pairwise_distances]{pairwise_distances()}},
\code{\link[=tales_assign_domain_codes]{tales_assign_domain_codes()}},
\code{\link[=tales_compare_distal]{tales_compare_distal()}},
\code{\link[=tales_group_hclust]{tales_group_hclust()}},
\code{\link[=tales_group_kmedoids]{tales_group_kmedoids()}},
\code{\link[=tales_tale_distances]{tales_tale_distances()}},
\code{\link[=validate_pairwise_distances]{validate_pairwise_distances()}}
}
\concept{pairwise distances}
